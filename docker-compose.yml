services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.vercel
    ports:
      - "8080:80"
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile.vercel
    env_file:
      - ./.env
    ports:
      - "8000:8000"
